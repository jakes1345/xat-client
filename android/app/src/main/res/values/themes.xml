<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Platform theme: Compose draws everything, so there is no need to pull
         in AppCompat or the Material Components library just for a theme. -->
    <style name="Theme.TrackDetect" parent="android:Theme.Material.NoActionBar">
        <item name="android:statusBarColor">#0E1116</item>
        <item name="android:navigationBarColor">#0E1116</item>
    </style>
</resources>
